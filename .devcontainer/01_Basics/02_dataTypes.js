"useStrict" // treat all js code as a new version

//alert(3+3); // we are using Nodejs not browser
//console.log(alert);

let name = "Alok"
let id = 07
let canVote = true

// null   = StandAlone datatype
// undefined
// Symbol = unique datatype mostly used in React-js
console.table([ typeof name,  typeof id, typeof canVote, typeof null , typeof undefined, typeof symbol])


